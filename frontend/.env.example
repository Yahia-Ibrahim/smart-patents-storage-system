# Base URL for the backend API.
# Dev leaves this unset and relies on the Vite proxy (/api -> :5000).
# In production, set it to the deployed API origin, e.g. https://api.example.com/api
VITE_API_URL=/api
